export const user = {
  // login: (
  //   result: DataFields,
  //   args: Variables,
  //   cache: Cache,
  //   info: ResolveInfo
  // ) => {
  //   betterUpdateQuery<LoginMutation, MeQuery>(
  //     cache,
  //     { query: MeDocument },
  //     result,
  //     (result, query) => {
  //       if (result.login.errors) return query
  //       const { refreshToken, token } = result.login.token
  //       localStorage.setItem("refreshToken", refreshToken)
  //       localStorage.setItem("token", token)
  //       return { ...query }
  //     }
  //   )
  // },
}
